# SAAS_GAP_MATRIX — gaps found and their disposition

| # | Component | Current state (evidence) | Expected SaaS state | Gap severity | Disposition |
|---|-----------|--------------------------|---------------------|--------------|-------------|
| 1 | Super admin UX | Same clinic dashboard for `super_admin`; no router in SPA | Dedicated control plane | CRITICAL | Fixed: platform bootstrap branch + `PlatformConsole` (overview/tenants 360/plans/usage/support/users/audit) |
| 2 | Platform API | 4 routes, authz inside controller, no capability model | Capability-guarded control plane API | HIGH | Fixed: `EnsurePlatformAccess` middleware + `PlatformAuthorizer` capabilities (`ris.platform_roles`) |
| 3 | Plan limits | Stored, never enforced (`max_users`, `max_studies_per_month` unused in code) | Server-side enforcement | HIGH | Fixed: `EntitlementService::enforce` on study booking + staff creation (403 `quota_exceeded`) |
| 4 | Entitlements / features | `plans.features` JSON unread | Feature flags: default → plan → tenant override; API-enforced | HIGH | Fixed: `FeatureResolver` + `denyFeatureUnlessEnabled` on inventory/dicom/dispatch writes |
| 5 | Usage metering | None | Tenant-attributed, transactional meters | HIGH | Fixed: `usage_counters` (studies, reports) incremented in the domain transaction; storage computed by `StorageMeter` (real file sizes, tenant-keyed cache) |
| 6 | Tenant lifecycle | Status set by hand; trials never auto-expired; no offboarding | Lifecycle engine + sweep | HIGH | Fixed: `TenantLifecycleService` + `ris:subscription-sweep` (daily) + offboarding export + retention window + `ris:tenant-destroy` operator command |
| 7 | Multi-tenant users | Impossible (1 user = 1 `business_id`) | Global identity + memberships + explicit switching | HIGH | Fixed: `tenant_memberships` + `POST /tenant/switch` + tenant-scoped authorization (see `AUTHORIZATION_MODEL.md`) |
| 8 | Platform roles | Single `super_admin` type | Distinct platform roles (ops/billing/support/auditor) | MEDIUM | Fixed: `users.platform_role` + capability map; last-super-admin protection |
| 9 | Break-glass support | None (super admin had NO tenant access at all — platform staff could operate nothing clinically) | Audited, expiring support sessions | HIGH | Fixed: `support_sessions` + enter/leave context + full audit |
| 10 | Business::users() bug | Missing relation used by platform list → runtime exception | — | HIGH (latent crash) | Fixed: relation added; regression covered by platform tests |
| 11 | Audit attribution | `audit_logs` had no tenant column; platform actions invisible | Tenant-attributed + platform audit stream | MEDIUM | Fixed: `audit_logs.business_id` + `GET /platform/audit` + lifecycle event registry |
| 12 | Login for terminated tenants | Login always succeeded | Terminated/offboarding blocked at login; suspended/expired land on gate | MEDIUM | Fixed in `AuthController@login` + SPA `SubscriptionGateView` (402 handling) |
| 13 | SPA routing/gating | All 10 tabs for every role | Role + entitlement-gated surfaces | MEDIUM | Fixed: `ROLE_TABS` gating + entitlement-gated inventory/doctors tabs + platform console branch |
| 14 | Legacy single-tenant data | Businesses existed without control-plane records | First-class tenant registry | MEDIUM | Fixed: expand-migration backfills memberships + lifecycle history + usage baselines (no data loss) |
| 15 | Noisy-neighbor rate limits | Per-user `api` limiter only; no tenant-aggregate budget — one clinic's runaway automation could saturate shared capacity | Tenant-attributed API rate limits + platform-wide safety limits | MEDIUM | Fixed: `throttle:tenant` on the tenant data plane — aggregate per-active-tenant budget (`ris.tenant_api_rate_limit_per_minute`, default 2400/min), keyed on `getActiveBusiness()` so switched members/support sessions count against the operated clinic; isolation covered by `TenantRateLimitTest` |
| 16 | Deployment topology / regionalization | Every tenant implicitly on one undisclosed placement; no region, stamp, isolation profile, cluster or storage region; nothing recorded where a tenant physically lives | Declared placement catalog + per-tenant placement + re-placement, config-owned, audited | HIGH | Fixed: `businesses.region / deployment_stamp / isolation_profile / database_cluster / storage_region` (expand-only migration) + `ris.regions` / `ris.deployment_stamps` catalog + `GET /platform/infrastructure` and `PATCH /tenants/{id}/deployment` (`infrastructure.manage`, audited `tenant_deployment_updated`). Provisioning stamps the operator's declared default. Covered by `PlatformDeploymentTest` |
| 17 | White-labeling + custom domains | One hard-coded product brand for every tenant; no tenant-owned host could ever be served | Per-tenant presentation overrides + verified custom host registry, presentation-only | MEDIUM | Fixed: `tenant_branding` + `tenant_domains` (expand-only migration), `GET/PUT /tenants/{id}/branding` and domain add/verify/primary/remove. DNS TXT ownership proof `_polytronx-ris.<host>` = `polytronx-ris-verify=<tenant code>`; **the host is never an authorization input** — the tenant is always resolved from the session. Gated by the `branding` / `custom_domains` entitlements. Covered by `TenantBrandingTest` |
| 18 | Tenant integration registry | Integrations were ad-hoc per feature (DICOM nodes, notification templates) with no unified, secret-bearing registry and no honest health probe | One tenant/facility-scoped registry with encrypted credentials and real probes | HIGH | Fixed: `tenant_integrations` with `secrets` cast `encrypted:array` (never returned — presence + `••••••••` mask only) and genuine probes: `tcp` via `fsockopen`, `http` via `Http::get`, `config` reporting completeness **without** asserting delivery. Rotation audited (`tenant_integration_secret_rotated`). Covered by `TenantIntegrationTest` |
| 19 | Observability + operational tooling | Overview had counters only; a failed job's payload could not be inspected without DB access, a retry was impossible, and entitlement drift was invisible (the resolver silently ignores unknown keys) | Explained per-tenant health rollup + safe operational tooling | HIGH | Fixed: `TenantHealthService` (bulk aggregates; derived verdict with reasons; unmeasurable quota reports `null`, never a comfortable zero) + `JobInspector` (payload never returned; property names from **reflection on the job class**, never from the serialized payload) + `EntitlementReconciler` (reports drift; prunes only inert `stale` overrides, never contradicting ones) + real `queue:retry` / `queue:forget`. Covered by `PlatformOperationsTest` |
| 20 | `jobs` table missing | `config/queue.php` defaults to the `database` driver, but only `failed_jobs` was ever migrated — the configured queue could not accept a job | The configured queue driver must actually work | HIGH (latent) | Fixed: `2026_09_15_000400_create_jobs_table.php`. Nothing had exercised the queue (no queued work yet), so it went unnoticed until §81 required a retry to genuinely re-queue — a retry that cannot enqueue would have been a fake success |
| 21 | Route action integrity | A controller wired into `routes/api.php` without a `use` import resolved to the global namespace and 500'd the whole surface on first touch. Neither `php -l` (parses only) nor `tsc` (never reads routes) can see this | Every routed action must exist, and every platform route must be guarded | HIGH | Fixed: added the missing import (the `PlatformBrandingController` bug found in CI run #114) + `RouteIntegrityTest`, which walks `Route::getRoutes()` and fails on any action whose class or method does not exist, and asserts every `api/v1/platform` route carries the `platform` middleware |
| 22 | Invoice cash discount dropped at the API boundary | The SPA collected a cashier discount (amount/percent + reason) but the app shell discarded it (`void _discount`); only a free-text reason ever reached the server — the "Discount" field was decorative | The discount the cashier enters must fold into the server-authoritative invoice totals | HIGH | Fixed: `invoices.manual_discount` (expand migration) + `discountAmount` in `POST /studies/{id}/invoices`; `Invoice::recalculateTotals()` is now the single authority (the recalc was duplicated in two controllers, StudyController/BillingController) and caps the total at 0. Regression: `IntegrityRepairTest` |
| 23 | Report drafts duplicated on every save; addenda spliced into signed text | The SPA always POSTed new report rows (the `PUT /reports/{report}` endpoint had NO frontend consumer), so each "Save Draft" created another version row; addenda were appended into the signed `findings` string — an immutable signed document silently mutated client-side | Draft edits update the draft; addenda are new versioned, signed reports | HIGH | Fixed: SPA saves via `PUT /reports/{id}` when an unsigned draft exists; server rejects a second draft with 422; addendum flow POSTs a real addendum (type `addendum`, version+1, signed) and `markReported` is idempotent for reported/delivered studies (an addendum no longer 422s the pipeline). Regression: `IntegrityRepairTest` |
| 24 | "Download PDF" documents were client-side fabrications | The server PDF endpoints (`GET /reports/{id}/pdf`, `GET /invoices/{id}/pdf` with DomPDF blades) had ZERO frontend consumers; "official" printed reports/invoices were rendered client-side from client state — including a preview that fabricated `signedBy`/`signedAt` for unsaved drafts and a hard-coded "Dr. Tariq Mehmood" as referring doctor on the tax invoice | Official documents come from the server of record; client previews are labelled as such | HIGH | Fixed: Download PDF buttons now hit the server endpoints; the report preview never fakes a signature (unsigned drafts render "UNSIGNED DRAFT" in `pdfGenerator`); the invoice modal prints the study's real referrer. Regression: covered by existing PDF endpoint suites |
| 25 | Dose capture pre-filled invented clinical measurements | The completion modal pre-filled dose 7.8 mGy / DLP 345 / kVp 120 / mAs 220 / slice 280 (per modality), contrast volume 75 mL, flow 3.5 mL/s, cannula "Right Antecubital (20G)", plus scripted technique notes — a technologist could sign invented patient data into the dose record with zero keystrokes | Patient measurements start empty; the record requires real entries | HIGH | Fixed: all measurement fields start empty and are validated on submit (dose required > 0; contrast required when the service demands it); unknown contrast agents are recorded by free text instead of a hard-coded drug list; batch/expiry labels are honest (`unbatched` / `not recorded` instead of `BATCH-AUTO` / `2026-12-31`). The "auto-deducts on completion" claim was verified true server-side (`InventoryService::deductForStudy`) |
| 26 | Queue-board calls existed only in one browser | "Call next patient" played a local chime and kept history in component state — other terminals never saw the call and it left no audit trail | Calls persist server-side: shared "now serving" across terminals + audited | MEDIUM | Fixed: `POST /studies/{id}/transition {action: 'call'}` stamps `appointments.called_at` + audit `queue_patient_called`; the TV board renders the server call log. Expand migration; regression: `IntegrityRepairTest` |
| 27 | "Verify QC & Push to Radiologist" wrote status into notes | The PACS sign-off appended the literal text `[PACS QC reviewed by technologist]` to `notes` — workflow state smuggled through free text, no transition, no gate | QC handoff is the real `acquired → reading` pipeline step | MEDIUM | Fixed: the button performs the `send_to_reading` transition (previously the legal `Acquired→Reading` step and `StudyWorkflowService::sendToReading` were unreachable from HTTP); `reading_at` is now stamped honestly (it previously borrowed `acquired_at`). Regression: `IntegrityRepairTest` |
| 28 | Appointment reminders could never work | The scheduled command parsed Y-m-d study dates as d-m-Y, required an exact-minute match against a 5-minute schedule, crashed on a missing `defult_timezone` setting, logged to a channel keyed to settings the SPA cannot set, and queued `CommonEmailTemplate` whose blade view DID NOT EXIST on a host with no queue worker — reminders silently never fired | Opt-in, idempotent, deliverable reminders against the modern schema | HIGH | Fixed: `app:appointment-reminder` rewritten — clinic-profile opt-in (`sendAppointmentReminders`, `reminderHours` settings + Settings UI), window-based (default 24 h), `reminder_sent_at` dedup marker, placeholder `@patients.local` addresses never mailed (but stamped), synchronous send (no worker on the Hostinger topology), audited `appointment_reminder_sent`, real `emails.appointment_reminder` blade + `AppointmentReminderMail`. Regression: `IntegrityRepairTest` |
| 29 | Multi-tenant integrity holes in validation | Screening answers with ANOTHER clinic's question ids were silently skipped — which CLEARED the safety gate; service/template/screening `modalityId` validated `exists:modalities,id` globally; adverse-reaction `appointmentId` accepted cross-tenant; SKU codes were globally unique (tenant B could not reuse clinic A's code and validation leaked its existence) | Everything tenant-scoped and strict | HIGH | Fixed: screening questions tenant-scoped and unknown/foreign ids → 422 (never a silent safety clearance); modality rules `Rule::exists()->where(business_id)`; adverse-reaction appointment must belong to the tenant (404); SKU codes unique per tenant; opening stock now recorded as REAL `stock_in` movements via `InventoryService` (the SPA fabricated a `local-…` ledger row because the server created none). Regressions: `IntegrityRepairTest` |
| 30 | Service deletion could cascade study history away | `appointments.service_id` FK is `ON DELETE CASCADE`; `destroyService` had no reference guard (modality deletion had one) | A referenced service cannot be deleted | HIGH | Fixed: 422 when studies exist (`deactivate instead`); unreferenced services delete cleanly. Regression: `IntegrityRepairTest` |
| 31 | Auth/UX honesty sweep | `type=customer` accounts could authenticate onto the staff data plane (full PHI bootstrap); notification writes were optimistic with swallowed failures; the terminal lock vanished on reload; the notification sound toggle and the clinical-presence picker did nothing; inventory KPI slogans ("100% Stocked") were hard-coded; commission settlements printed a hard-coded 12% and "August 2026"; booking modal pre-fabricated token/MRN/room and magic fallback ids; printed barcodes were decorative stripes; station-to-station consistency gaps in toasts (success before await) | No control without a server truth; no success claim before the server confirms | MEDIUM | Fixed: customer login 403; optimistic notification writes roll back on failure; lock persists in sessionStorage; sound toggle chimes on real new notifications (triple-blip on critical); presence picker removed (no backend); KPI sub-stats derived from data; commission rate is a real clinic setting (`referralCommissionPercent`, configurable in Settings) and the settlement period is the current month; booking defers all identifiers/room to the server; real scannable Code128 barcodes (jsbarcode) on wristbands/tokens/receipts/invoices; toasts fire only after the response. Regressions: `IntegrityRepairTest` + existing suites |

| 32 | Radiologists could reach booking entry points in the SPA | The server already denied `appointment create` to radiologists (403), but the SPA rendered the global-search "+ New Booking", the dashboard "Book Diagnostic Study" module and the reception walk-in button to every tenant role — security by 403 toast | Server-issued permission flags drive every booking affordance | MEDIUM | Fixed: bootstrap payload now carries `permissions[]` (`TenantAuthorizer::permissionsFor`, server-issued like roles/capabilities); every booking entry point renders only with `appointment create` and the single opener re-checks it. Radiologists/technologists see no booking affordance; the API remains the enforcement point. Regressions: `RbacTest` (radiologist 403 incl. crafted cross-tenant payloads, config CRUD denials) |
| 33 | Modality suites were schema-only; booking wrote a fake room | `rooms`/`equipment_downtimes` tables existed with models and seeded permissions but no API, no UI, no seed; `StudyController@store` auto-assigned the first room or wrote the hard-coded literal `'Room 1'` into `appointments.room_number` when none existed — an invented value presented as facility configuration | Tenant-managed imaging suites, selectable per modality at booking, honest when absent | HIGH | Fixed: rooms CRUD (`POST/PUT/DELETE /rooms`, `room *` permissions), suites in the bootstrap payload, "Modality Suite" selector in the booking form (modality-filtered, optional auto-assign), nullable `appointments.room_id` (expand migration) and NO fabricated room when none is configured (empty state + admin shortcut instead). Delete guard when studies reference the suite. Regressions: `BookingRoomTest`, `MastersConfigTest` |
| 34 | Payment methods were a hard-coded 5-value enum; booking captured no money | `invoice_payments.method` validated `in:cash,card,bank,mobile,insurance` in the controllers with matching hard-coded dropdowns in the SPA; the booking flow auto-issued an invoice but had no payment status/method/amount capture | Tenant-configured payment methods everywhere + validated booking-time settlement against the auto-issued invoice | HIGH | Fixed: `payment_methods` table (per-tenant, unique code, active flag) seeded idempotently in the migration (existing tenants) AND `TenantBootstrap` (new tenants) with the five legacy codes so historical payment rows stay valid; admin management UI (Catalog & Forms → Payment Methods, code immutable after creation); POS + booking validate against the tenant's ACTIVE methods only; the booking form captures payment status/amount/method/reference plus an `invoice edit`-gated cash discount, all validated and settled server-side inside the booking transaction (shared `InvoicePaymentService`), audited (`payment_recorded`). Regressions: `StudyBookingPaymentTest`, `MastersConfigTest` |

Not gaps (verified as already correct): pooled `business_id` scoping on all domain tables; server-side tenant resolution (client business ids never trusted); per-tenant laratrust roles; tenant-scoped settings caches; export isolation; polling-only realtime (no cross-tenant channels); no search engine to isolate (client-side filtering of tenant-scoped payloads); sync queue (no async boundary to isolate).
